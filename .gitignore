# Logs
logs
*.log
.serverless
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.serverless
uploads
dist-ssr
*.local
dist
.vscode/launch.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.zip
To ignore the compiled C++ executable files in your Git repository, follow these steps:  

### **1. Open/Create `.gitignore` File**  
If your project doesn’t have a `.gitignore` file, create one in the root of your repository:  

```bash
touch .gitignore
```

### **2. Add These Entries to `.gitignore`**  
Open the `.gitignore` file in a text editor and add the following lines:

# Ignore specific extensions (like .out)
*.out

# Ignore all compiled C++ binaries in the current folder
*.exe
*.o
*.out
*.bin